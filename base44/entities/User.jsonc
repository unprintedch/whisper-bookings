{
  "type": "object",
  "properties": {
    "custom_role": {
      "type": "string",
      "enum": [
        "full_access",
        "agency"
      ],
      "default": "full_access",
      "description": "R\u00f4le personnalis\u00e9 de l'utilisateur"
    },
    "agency_id": {
      "type": "string",
      "description": "ID de l'agence associ\u00e9e (pour les utilisateurs agency)"
    },
    "client_view_mode": {
      "type": "string",
      "enum": [
        "clients",
        "reservations"
      ],
      "default": "clients",
      "description": "Preferred view mode for the Clients page"
    },
    "client_view_column_visibility": {
      "type": "object",
      "description": "Column visibility preferences for the reservations table"
    }
  },
  "name": "User",
  "required": []
}